{
    "version": 2,
    "builds": [
      { 
        "src": "api/app.py", 
        "use": "@vercel/python",
        "config": {
          "maxDuration": 30
        }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "api/app.py" }
    ]
}